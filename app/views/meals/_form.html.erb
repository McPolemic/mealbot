<%= form_with(model: meal, local: true) do |form| %>
  <% if meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meal.errors.count, "error") %> prohibited this meal from being saved:</h2>

      <ul>
        <% meal.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :breakfast %>
    <%= form.text_area :breakfast %>
  </div>

  <div class="field">
    <%= form.label :lunch %>
    <%= form.text_area :lunch %>
  </div>

  <div class="field">
    <%= form.label :dinner %>
    <%= form.text_area :dinner %>
  </div>

  <div class="field">
    <%= form.label :snacks %>
    <%= form.text_area :snacks %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
